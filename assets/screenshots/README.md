Drop screenshots here, then uncomment the `<img>` block in the main README's "What it does" section.

Suggested shots (PNG, ~1200-1600px wide is plenty):

- `search.png` — the landing page with the pre-filled example query, or a results list with eligibility badges visible
- `eligibility.png` — the admission-guide drawer open, showing the structured requirements and a verdict

Filenames are just a suggestion — match whatever you use to the `src=` path in the README.
